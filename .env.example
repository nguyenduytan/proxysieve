# Configuration keys recognized by the M1 config CLI.
# Never put live credentials in this example or commit your local .env.
# PROXYSIEVE_LOG_LEVEL=info
# PROXYSIEVE_DATA_DIR=./data
# PROXYSIEVE_SECRET_WEBHOOKS_PRIMARY=replace-with-a-random-signing-key
