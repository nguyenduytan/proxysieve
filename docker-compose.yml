services:
  proxysieve:
    build: .
    image: proxysieve:dev
    command: ["version"]
    read_only: true
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    # M0 is CLI-only. No network listeners, port mappings, or restart loop.
