services:
  proxysieve:
    build: .
    image: proxysieve:dev
    command: ["version"]
    read_only: true
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    # Artifact smoke test only; gateway deployment still requires an explicit config and writable data volume.
